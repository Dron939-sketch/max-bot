# Основные зависимости для бота MAX
maxibot>=0.1.0
aiohttp>=3.9.0
python-dotenv>=1.0.0
asyncpg>=0.31.0  # <--- ИЗМЕНЕНО: нужна версия 0.31.0 для Python 3.14
python-dateutil>=2.8.2
pytz>=2023.3

# FastAPI и веб-сервер
fastapi>=0.115.0    
uvicorn==0.24.0
python-multipart==0.0.6

# Асинхронные патчи и совместимость с Python 3.14
nest_asyncio==1.6.0
sniffio>=1.3.0
anyio>=4.4.0  # КРИТИЧЕСКИ ВАЖНО: версия 4.4.0+ исправляет баг с Python 3.14

# Дополнительные зависимости для работы
requests>=2.31.0
pydantic>=2.5.0
jinja2>=3.1.2
httpx>=0.25.0
websockets>=12.0

# Для работы с голосом (если используется)
pydub>=0.25.1
SpeechRecognition>=3.10.0  # <--- ИСПРАВЛЕНО: убрал пробел

# Для работы с погодой (если используется)
geopy>=2.4.0
timezonefinder>=6.1.0

# Для Python 3.14 добавляем async_timeout (может понадобиться)
async-timeout>=4.0.0
